|include <stdlib> as *|
|include <stdio> as *|

class main implements runtime {

    private static ErrorHandle err = new ErrorHandle();

    public static func main(int argc, char** argv) => int {
        out.println("Hello, World");
        return 0;
    }
}